# ── Dependencies ──
node_modules/

# ── Environment variables (NEVER commit these) ──
.env
.env.local
.env.development
.env.production
.env.test
.env*.local

# ── Logs ──
*.log
npm-debug.log*
yarn-debug.log*
error.log

# ── OS junk ──
.DS_Store
Thumbs.db

# ── Prisma generated ──
/generated/prisma
prisma/migrations/

# ── Temp / backup files ──
*.tmp
*.bak
*.backup
*.orig
*_old.*
*_backup.*
*.txt

# ── Sensitive files ──
users.json
formatted_credentials*
credentials*.json
accounts*.json

# ── Build outputs ──
dist/

# ── Python ──
__pycache__/
*.pyc
